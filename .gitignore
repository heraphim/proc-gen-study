# Derived — rebuild with `npm run migrate`
data/*.db
data/*.db-journal
data/*.db-wal
data/*.db-shm

# Derived — rebuild with `npm run build`
dist/

node_modules/
.DS_Store

# Generated by the registry poller when asked for a report
poll-report.md
research-report.md
