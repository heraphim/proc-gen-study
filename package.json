{
  "name": "procgen-catalogue",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Research catalogue of procedural generation techniques. SQLite-backed, served locally.",
  "scripts": {
    "migrate": "node scripts/migrate.js",
    "check-samples": "node scripts/check-samples.js",
    "build": "node scripts/build-static.js",
    "preview": "node scripts/serve-dist.js",
    "start": "node server.js",
    "dev": "node --watch server.js",
    "check-register": "node scripts/check-register.js",
    "poll": "node scripts/poll-registries.js"
  },
  "engines": {
    "node": ">=22.5"
  }
}
